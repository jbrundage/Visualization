{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "file:///${workspaceRoot}/demos/catalog/index.html",
            "webRoot": "${workspaceRoot}",
            "runtimeArgs": [
                "--disable-web-security"
            ]
        }
    ]
}